// import React from 'react'

const Group = ({ groupId , title, icon, notificationsNumber,  }: any) => {
 
//     // aqui tenemos que utilizar redux para que podamos utilizar distribuir las funciones que se encuentran todas en el archivo chatsContent y distribuirlas entre todos los minicomponenetes que vamos a crear   

//     return (
//         <>
//             <div id={groupId} onClick={CloseMenuOpenGroupChatHandler}>{group.group_icon}</div>
//             <h2 id={group.group_id} className="w-auto h-6 whitespace-nowrap overflow-hidden " onClick={CloseMenuOpenGroupChatHandler}>{group.group_name}</h2>
//             <p className={`${notifications_number > 0 ? '' : 'hidden'} w-6 h-6 flex flex-row justify-center  items-center ml-auto rounded-full bg-blue-500 text-sm`}>{notifications_number}</p>
//         </>
//     )
}

export default Group